export const auth = {
    namespaced: true,

    actions: {
        login(_, siteId) {
            console.log('login', siteId)
        }
    },
    getters: {
    },
    mutations: {
    },
    state: {
    }
}